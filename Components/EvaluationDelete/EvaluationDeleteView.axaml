<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:evaluationDelete="clr-namespace:SubjectHelper.Components.EvaluationDelete"
             mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="450"
             x:Class="SubjectHelper.Components.EvaluationDelete.EvaluationDeleteView"
             Width="400"
             x:DataType="evaluationDelete:EvaluationDeleteViewModel">
    
    <!-- <StackPanel Orientation="Horizontal" Spacing="0"> -->
    <!--      -->
    <!--     <Label Content="This will permanently delete the evaluation "/> -->
    <!--     <Label Content="{Binding Title}"/> -->
    <!--     <Label Content=" and all of its data. This action cannot be undone"/> -->
    <!--      -->
    <!-- </StackPanel> -->
    
    <TextBlock TextWrapping="Wrap">
        <TextBlock.Text>
            <MultiBinding StringFormat="This will permanently delete the evaluation '{0}' and all of its data. This action cannot be undone">
                <Binding Path="Title"/>
            </MultiBinding>
        </TextBlock.Text>
    </TextBlock>
    
</UserControl>
